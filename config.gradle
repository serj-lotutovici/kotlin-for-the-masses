project.ext {
  versions = [
      kotlin: '1.1.4-3'
  ]

  libraries = [
      kotlin : [
          core    : "org.jetbrains.kotlin:kotlin-stdlib:${versions.kotlin}",
          coreJre8: "org.jetbrains.kotlin:kotlin-stdlib-jre8:${versions.kotlin}",
          coreJs  : "org.jetbrains.kotlin:kotlin-stdlib-js:${versions.kotlin}"
      ],

      java   : [

      ],

      js     : [

      ],

      android: [

      ]
  ]

  usedPlugins = [
      kotlin : "org.jetbrains.kotlin:kotlin-gradle-plugin:${versions.kotlin}",
      android: "com.android.tools.build:gradle:3.0.0-beta4"
  ]
}
